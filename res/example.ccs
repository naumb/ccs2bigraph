* This is a very(!) simple example file.
* This (roughly) should be the result
* big A = Alt.(Send{x}.({x} | {y} | {z} | Nil) | Get{z}.Alt.Get{y}.Alt.Get{z}.({x} | {y} | {z} | Nil)) | Alt.(Get{x}.({x} | {y} | {z} | Nil));

A = ('x.0 + z.y.z.0) | x.0;
